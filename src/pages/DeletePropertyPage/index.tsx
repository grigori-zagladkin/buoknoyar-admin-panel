import { Heading, Stack } from "@chakra-ui/react";
import { FC } from "react";

const DeletePropertyPage: FC = () => {
    return (
        <Stack>
            <Heading as="h2">Удаление свойства</Heading>
        </Stack>
    );
};

export default DeletePropertyPage;
