import { Heading, Stack } from "@chakra-ui/react";
import { FC } from "react";

const UpdatePropertyPage: FC = () => {
    return (
        <Stack>
            <Heading as="h2">Обновление свойства</Heading>
        </Stack>
    );
};

export default UpdatePropertyPage;
